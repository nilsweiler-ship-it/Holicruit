# Holicruit is UI-first: the matching engine, scenario assessment, and
# marketplace are mocked behind clean service interfaces over fixtures.
# No environment variables are required to run the app (`npm run dev`).
#
# Add real service credentials here when those backends are wired up.
